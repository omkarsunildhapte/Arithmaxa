/** A single `{ value, label }` choice, shared by every `ion-select`/unit
 *  picker across the tools pages (unit, percentage-type, currency). */
export interface SelectOption {
  value: string;
  label: string;
}
