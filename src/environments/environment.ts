export const environment = {
  production: true,
  // Set to your deployed arithmaxa-backend URL before shipping — see
  // arithmaxa-backend/README.md's Render deployment section. AI Chat's
  // cloud path (and the OpenRouter key itself) now lives entirely behind
  // that backend; the app no longer holds any AI provider key at all.
  backendUrl: '',
};
